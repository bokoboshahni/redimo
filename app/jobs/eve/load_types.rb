# frozen_string_literal: true

module EVEStaticData
  # Loads EVE type data from the EVE static data export and download of types from ESI.
  class LoadTypes
    def initialize(sde_path, esi_types_file); end
  end
end
